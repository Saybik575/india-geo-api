{
  "name": "india-village-explorer-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.7.0",
    "pg": "^8.20.0",
    "redis": "^5.11.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.9",
    "tailwindcss": "^4.2.2"
  }
}
